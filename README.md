# snapchat-saved-memories
Download data you've requested from snapchat, which is accessed via urls and stored in a json file

How to run the file:
download relevant libraries mentioned in the code

Run this in terminal/cmd:
python filename.py /path/to/your/file.json
